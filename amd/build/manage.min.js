define("local_coursereminders/manage", ["exports"], function (_exports) {
    "use strict";

    Object.defineProperty(_exports, "__esModule", {value: true});
    _exports.init = void 0;

    var init = function init() {
        var form = document.getElementById('local-coursereminders-manageform');
        if (!form) {
            return;
        }
        var selectAll = form.querySelector('[data-action="selectall"]');
        if (!selectAll) {
            return;
        }
        selectAll.addEventListener('change', function () {
            form.querySelectorAll('input[name="ruleids[]"]').forEach(function (checkbox) {
                checkbox.checked = selectAll.checked;
            });
        });
    };

    _exports.init = init;
});
